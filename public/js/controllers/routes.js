'use strict';
var app = app || {};

page('/', () => {
  // TODO: hide everything but what we want on the home page
});

page('/about', () => {
  // TODO: hide everything except about us
});

page('/portfolio', () => {
  // TODO: hide everything but portfolio page
});

page();
